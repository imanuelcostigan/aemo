# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
r:
  - oldrel
  - release
  - devel
cache: packages
matrix:
  include:
    - os: linux
      sudo: false
    - os: osx
      osx_image: xcode7.3
      latex: false

